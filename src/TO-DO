4.add picture function that will photograph screen only when allowed
5.add lock function to some og the elements in some of the arrays
7.create an array of all layers made of objects with key item and img src make sure array length flexable 8.
